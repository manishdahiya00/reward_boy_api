<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="robots" content="noindex, nofollow">
    <title>Login Admin Panel</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/core@1.0.0-beta17/dist/css/tabler.min.css">
    <style>
      body, html {
      height: 100%;
      background-color: #121212; /* Dark theme background */
      display: flex;
      justify-content: center;
      align-items: center;
      color: #e0e0e0; /* Light text color */
      }
      .card-container {
      max-width: 600px; /* Increased max width for wider fields */
      padding: 40px;
      background-color: #1e1e1e; /* Dark card background */
      border-radius: 8px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5); /* Subtle shadow */
      width: 100%; /* Make card full width */
      }
      .profile-img-card {
      width: 80px;
      height: 80px;
      margin: 0 auto;
      display: block;
      border-radius: 50%;
      }
      .form-signin .form-control {
      margin-bottom: 15px;
      height: 48px;
      background-color: #2c2c2c; /* Dark input background */
      color: #e0e0e0; /* Light input text */
      border: 1px solid #444; /* Border color for inputs */
      font-size: 16px;

      }
      .form-signin .form-control:focus {
      border-color: #616161; /* Lighter border on focus */
      background-color: #333; /* Darker background on focus */
      color: #fff;
      }
      .btn-signin {
      font-weight: 600;
      font-size: 18px;
      height: 48px;
      background-color: #007bff; /* Bright button */
      color: white;
      width: 100%; /* Make button full width */
      }
      .btn-signin:hover {
      background-color: #0056b3; /* Darker on hover */
      }
      .card-header {
      text-align: center;
      padding-bottom: 20px;
      }
    </style>
  </head>
  <body>
    <div class="page page-center">
      <div class="container-tight py-4">
        <div class="card card-container">
          <div class="card-header">
            <img class="profile-img-card" src="https://www.shareicon.net/data/2015/09/08/98065_password_512x512.png" alt="Profile Image">
            <h2 class="h3 text-center mt-3">Admin Login</h2>
          </div>
          <div class="card-body">
            <%= form_tag '/admin', class: "form-signin" do %>
              <div class="mb-3">
                <%= text_field_tag :username, "", class: 'form-control', id: "inputEmail", placeholder: 'Enter Email', required: true %>
              </div>
              <div class="mb-3">
                <%= password_field_tag :password, "", class: 'form-control', id: "inputPassword", placeholder: 'Enter Password', required: true %>
              </div>
              <div class="form-footer">
                <button type="submit" class="btn btn-primary w-100 btn-signin">Sign in</button>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@tabler/core@1.0.0-beta17/dist/js/tabler.min.js"></script>
  </body>
</html>
